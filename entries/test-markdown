# 🌱 Это тестовая запись в Markdown!

Привет! Я пишу это **прямо в GitHub** без HTML. Смотри, какие возможности:

## Форматирование
- **Жирный текст** и *курсив* работают
- Ссылки: [мой цифровой сад](https://genapoleno.github.io)
- Код: `console.log('Привет, сад!')`
- Цитаты:
  > Цифровой сад — это не идеальный блог. Это место, где мысли растут, меняются и иногда вымирают.

## Списки
1. Первый пункт
2. Второй пункт
   - Подпункт
   - Ещё подпункт

## Код
```js
// Пример кода на JavaScript
function growGarden() {
  console.log('Сад растёт!');
  return true;
}
